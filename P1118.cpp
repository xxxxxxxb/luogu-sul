#include <iostream>
using namespace std;
#define maxn 13
int n,sum,ansn[maxn];
void dfs(int x)
{
    if(x > n){
        
    }
}
int main()
{
    std::ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> sum;
    return 0;
}