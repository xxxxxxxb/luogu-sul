#include <bits/stdc++.h>
using namespace std;

priority_queue<int> big_heap;
priority_queue<int, vector<int>, greater<int>> small_heap;
int a[(int)2e5 + 5], m, n, q, k = 1;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> m >> n;
    for (int i = 1; i <= m; i++)
        cin >> a[i];
    for (int i = 1; i <= n; i++)
    {
        cin >> q;
        for(int j = k;j <= q;j++)
        {
            
        }
    }
    return 0;
}